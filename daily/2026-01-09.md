# 2026-01-09

## 基本習慣

- [x] ラジオ体操
- [ ] 洗顔・保湿
- [x] シダキュア
- [x] Typnix 練習

## エクササイズ

- [x] スクワット 20 回完了
- [x] 腕立て伏せ 10 回完了
- [x] 腹筋 20 回完了
- [ ] エクササイズ動画

## 学習

### Sociomedia ヒューマンインターフェース ガイドライン

- [x] 今日のトピック: 9. モードレス(Modelessness)
- URL: https://www.sociomedia.co.jp/9094
- メモ: UI におけるモードとは、現在可能な操作を限定したり、操作手順を固定的に矯正する状態。モードがあると、ユーザーは作業の途中で考えを変えて別のことをする際にまずモードから抜けるという操作が必要になる。モードレスであればユーザーは自由な順序で作業を行うことができる。

### Ruby 4.0 Documentation - Array Instance Methods

- [x] 今日のメソッド: `[]=`
- URL: https://docs.ruby-lang.org/en/4.0/Array.html#method-i-5B-5D-3D
- メモ:

```ruby
self[index] = object → object
self[start, length] = object → object
self[range] = object → object
```

#### コード例・補足

```ruby
# Assigns elements in self, based on the given object; returns object.
# 与えられたオブジェクトに基づいて、自身の要素を割り当てます。オブジェクトを返します。

a_orig = [:foo, 'bar', 2]

# With argument index.
# 引数インデックス付き
a = a_orig.dup # dupはオブエクトの内容をコピーするメソッド
a[0] = 'foo' # => "foo"
a # => ["foo", "bar", 2]
a = a_orig.dup
a[7] = 'foo' # => "foo"
a # => [:foo, "bar", 2, nil, nil, nil, nil, "foo"]

# With arguments start and length.
a = a_orig.dup
a[0, 2] = 'foo' # => "foo"
a # => ["foo", 2]
a = a_orig.dup
# If start is non-negative and outside the array ( >= self.size), extends the array with nil, assigns object at offset start, and ignores length:
# 開始位置が非負で、配列の範囲外（self.size 以上）の場合、配列をnilで拡張し、指定されたオフセットにオブジェクトを割り当て、length は無視します。
a[6, 50] = 'foo' # => "foo"
a # => [:foo, "bar", 2, nil, nil, nil, "foo"]
# If length is zero, shifts elements at and following offset start and assigns object at offset start:
# 開始位置が非負で、配列の範囲外（self.size 以上）の場合、配列をnilで拡張し、指定されたオフセットにオブジェクトを割り当て、length は無視します。
a[1, 0] = 'foo' # => "foo"
a # => [:foo, "foo", "bar", 2]

# With argument range.
a = a_orig.dup
a[0..1] = 'foo' # => "foo"
a # => ["foo", 2]
a = a_orig.dup
a[6..50] = 'foo' # => "foo"
a # => [:foo, "bar", 2, nil, nil, nil, "foo"]
```
