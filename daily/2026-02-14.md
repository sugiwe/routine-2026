# 2026-02-14

## 基本習慣

- [x] ラジオ体操
- [x] 洗顔・保湿
- [x] シダキュア
- [x] Typnix 練習

## エクササイズ

- [x] スクワット 20 回完了
- [x] 腕立て伏せ 20 回完了
- [x] 腹筋 20 回完了
- [ ] エクササイズ動画

## 学習

### Sociomedia ヒューマンインターフェース ガイドライン

- [x] 今日のトピック: 45. 値を入力させるのではなく結果を選ばせる (Results Rather Than Values)
- URL: https://www.sociomedia.co.jp/9464
- メモ: 自分で細かいパラメーターを設定していくよりも、選択肢から選ぶことを好むユーザーが多い。その通りな気もするし、最終的に細かい調整をしたくなる気もする。
- 詳細ノート: [learning/sociomedia-hig/045-results-rather-than-values.md](../learning/sociomedia-hig/045-results-rather-than-values.md)

### Ruby 4.0 Documentation - Array Instance Methods

- [x] 今日のメソッド: `find_index`
- URL: https://docs.ruby-lang.org/en/4.0/Array.html#method-i-find_index
- メモ:
- 詳細ノート: [learning/ruby-array-methods/045-find-index.md](../learning/ruby-array-methods/045-find-index.md)

#### コード例・補足

```ruby
# find_index(object) → integer or nil
# find_index {|element| ... } → integer or nil
# find_index → new_enumerator

# Returns the zero-based integer index of a specified element, or nil.
# 指定された要素の0始まりの整数インデックスを返します。見つからない場合はnilを返します。
# With only argument object given, returns the index of the first element `element` for which `object == element`:
# 引数`object`のみが指定された場合、`object`と等しい最初の要素のインデックスを返します。
a = [:foo, 'bar', 2, 'bar']
a.find_index('bar') # => 1

# Returns nil if no such element found.
# 該当する要素が見つからない場合は、nilを返します。
# With only a block given, calls the block with each successive element; returns the index of the first element for which the block returns a truthy value:
# ブロックのみが与えられた場合、ブロックを後続の各要素で呼び出します。ブロックが真値を返した最初の要素のインデックスを返します。
a = [:foo, 'bar', 2, 'bar']
a.find_index {|element| element == 'bar' } # => 1

# Returns nil if the block never returns a truthy value.
# ブロックが一度も真値を返さなかった場合、nilを返します。
# With neither an argument nor a block given, returns a new Enumerator.
# 引数もブロックも与えられなかった場合、新しい`Enumerator`を返します。

# Related: see Methods for Querying.
# Also aliased as: index
```
