# 2026-02-25

## 基本習慣

- [x] ラジオ体操
- [ ] 洗顔・保湿
- [x] シダキュア
- [x] Typnix 練習

## エクササイズ

- [x] スクワット 20 回完了
- [x] 腕立て伏せ 20 回完了
- [x] 腹筋 20 回完了
- [ ] エクササイズ動画

## 学習

### Sociomedia ヒューマンインターフェース ガイドライン

- [x] 今日のトピック: 56. 可能性と確率を区別する (Segregate Possible From Probable)
- URL: https://www.sociomedia.co.jp/9540
- メモ: 「滅多にない事を考慮しすぎて通常の利用を面倒にしてはいけない。プログラマーはエッジケースを重視するがデザイナーはメインケースを重視すべき」とのこと。深い話だな👀 パレートの法則を思い出した。プログラマは、自動テストなどの活用で細かい検証を実施可能だからエッジケースを網羅すべきだが、少ないコストで最大のパフォーマスを出す、多くの人に誤解をさせない、マイノリティな要望は控えめに搭載してカバーする、みたいなことかな。
- 詳細ノート: [learning/sociomedia-hig/056-segregate-possible-from-probable.md](../learning/sociomedia-hig/056-segregate-possible-from-probable.md)

### Ruby 4.0 Documentation - Array Instance Methods

- [x] 今日のメソッド: `join`
- URL: https://docs.ruby-lang.org/en/4.0/Array.html#method-i-join
- メモ:
- 詳細ノート: [learning/ruby-array-methods/056-join.md](../learning/ruby-array-methods/056-join.md)

#### コード例・補足

```ruby
# Returns the new string formed by joining the converted elements of self; for each element `element`:
# selfの変換された各要素を結合して新しい文字列を返します。
# - Converts recursively using `element.join(separator)` if `element` is a `kind_of?(Array)`.
# - `element`が配列の場合、`element.join(separator)` を用いて再帰的に変換します。
# - Otherwise, converts using `element.to_s`.
# - それ以外の場合は、`element.to_s` を使用して変換されます。

# With no argument given, joins using the output field separator, `$,`:
# 引数を指定しない場合、出力フィールドセパレータである「$,」を使って結合します。
a = [:foo, 'bar', 2]
$, # => nil
a.join # => "foobar2"

# With string argument `separator` given, joins using that separator:
# 文字列引数`separator`を指定すると、そのセパレータを使って結合します。
a = [:foo, 'bar', 2]
a.join("\n") # => "foo\nbar\n2"

# Joins recursively for nested arrays:
# ネストされた配列を再帰的に結合します。
a = [:foo, [:bar, [:baz, :bat]]]
a.join # => "foobarbazbat"
```
